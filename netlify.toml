[build]
  base = "frontend"
  command = "npm run build && node scripts/netlify-routes.mjs"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# scripts/netlify-routes.mjs writes dist/_redirects using ADF_BACKEND_URL.
